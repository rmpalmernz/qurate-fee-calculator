# Memory: architecture/deployment-routing
Updated: 2026-02-03

The application is deployed at fee.qurate.com.au. Access is controlled at the root '/' level, requiring either a valid 30-day token or the development bypass parameter (?dev=true). The application is designed to function as a standalone calculator served from the root of its domain.
